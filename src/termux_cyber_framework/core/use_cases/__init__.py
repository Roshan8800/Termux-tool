# Use Cases package
# This package contains the application-specific business rules. It orchestrates
# the flow of data between the domain entities and the external world via ports.
# The use cases define the interfaces (ports) that adapters must implement.
