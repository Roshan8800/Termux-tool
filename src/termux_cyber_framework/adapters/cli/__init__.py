# CLI Adapter package
# This is a primary (or driving) adapter that provides a command-line
# interface for users to interact with the application. It calls the
# use cases in the core to execute commands.
