# Database Adapter package
# This is a secondary (or driven) adapter that implements the data persistence
# port defined in the core. It handles communication with a specific database,
# such as SQLite, PostgreSQL, or an in-memory store.
